apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: customer-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: jailtonjunior.net
      http:
        paths:
          - pathType: Prefix
            path: /customers
            backend:
              service:
                name: customer-api-service
                port:
                  number: 8000
